<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CTG Referral Portal | Concussion Therapy Group</title>
    <!-- FontAwesome for professional medical and tech icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        /* Tech-Vibe & Identity Variables */
        :root {
            --primary-blue: #0f172a;
            --accent-teal: #0ea5e9;
            --bg-light: #f8fafc;
            --bg-azure: #f0f7ff; 
            --bg-slate: #edf2f7; 
            --text-dark: #0f172a;
            --text-muted: #475569;
            --white: #ffffff;
            --glass-strong: rgba(255, 255, 255, 0.85);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', 'Segoe UI', system-ui, -apple-system, sans-serif;
            background-color: var(--bg-light);
            color: var(--text-dark);
            line-height: 1.6;
            overflow-x: hidden;
        }

        /* --- DYNAMIC PLEXUS BACKGROUND --- */
        #plexus-canvas {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            background: radial-gradient(circle at 50% 50%, #f1f5f9 0%, #e2e8f0 100%);
        }

        /* Header Navigation */
        header {
            background: rgba(255, 255, 255, 0.85);
            backdrop-filter: blur(20px);
            padding: 18px 8%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid rgba(14, 165, 233, 0.1);
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        .logo {
            font-weight: 800;
            font-size: 20px;
            color: var(--primary-blue);
            letter-spacing: -0.5px;
            text-transform: uppercase;
        }

        .logo span {
            color: var(--accent-teal);
            margin: 0 5px;
        }

        .btn-header {
            background: var(--primary-blue);
            color: var(--white);
            padding: 12px 28px;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 600;
            font-size: 14px;
            transition: 0.3s;
        }

        .btn-header:hover {
            background: var(--accent-teal);
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(14, 165, 233, 0.3);
        }

        /* Hero Section */
        .hero {
            display: grid;
            grid-template-columns: 1fr 1.1fr;
            gap: 40px;
            padding: 100px 8% 180px;
            align-items: center;
            min-height: 85vh;
            position: relative;
        }

        .hero-content h1 {
            font-size: clamp(40px, 5vw, 62px);
            line-height: 1.1;
            margin-bottom: 25px;
            color: var(--primary-blue);
            font-weight: 900;
            letter-spacing: -1.5px;
        }

        .hero-content p {
            font-size: 20px;
            color: var(--text-muted);
            margin-bottom: 40px;
            max-width: 500px;
        }

        .hero-btns {
            display: flex;
            gap: 15px;
        }

        .btn-hero {
            padding: 18px 36px;
            font-size: 16px;
            font-weight: 700;
            border-radius: 12px;
            text-decoration: none;
            transition: 0.3s;
        }

        .btn-primary { background: var(--accent-teal); color: var(--white); }
        .btn-secondary { background: var(--white); color: var(--primary-blue); border: 1px solid #e2e8f0; }

        .btn-hero:hover { transform: translateY(-3px); box-shadow: 0 10px 20px rgba(0,0,0,0.05); }

        .hero-image {
            display: flex;
            justify-content: center;
            align-items: center;
            position: relative;
        }

        /* Evolving Brain Animation */
        .hero-image img {
            width: 100%;
            max-width: 550px;
            border-radius: 24px;
            z-index: 2;
            animation: evolutionPulse 6s infinite ease-in-out;
            transition: 0.5s;
        }

        @keyframes evolutionPulse {
            0% { 
                transform: scale(1);
                filter: drop-shadow(0 0 15px rgba(14, 165, 233, 0.2));
            }
            50% { 
                transform: scale(1.04);
                filter: drop-shadow(0 0 40px rgba(14, 165, 233, 0.6));
            }
            100% { 
                transform: scale(1);
                filter: drop-shadow(0 0 15px rgba(14, 165, 233, 0.2));
            }
        }

        /* --- STRAIGHT CUT DIVIDERS --- */
        .divider {
            position: absolute;
            left: 0;
            width: 100%;
            height: 100px;
            z-index: 5;
            pointer-events: none;
        }

        .divider-bottom {
            bottom: -1px;
        }

        /* Feature Cards Section */
        .why-section { 
            padding: 120px 8% 180px; 
            text-align: center; 
            background: rgba(237, 242, 247, 0.8); /* Tech Slate Tint */
            backdrop-filter: blur(8px);
            position: relative;
        }

        .section-title { font-size: 42px; margin-bottom: 60px; font-weight: 800; color: var(--primary-blue); letter-spacing: -1px; }

        .cards { display: grid; grid-template-columns: repeat(3, 1fr); gap: 30px; }

        .card {
            background: var(--glass-strong);
            backdrop-filter: blur(10px);
            padding: 50px 30px;
            border-radius: 28px;
            border: 1px solid rgba(255,255,255,0.7);
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            box-shadow: 0 10px 30px rgba(0,0,0,0.02);
        }

        .card:hover { transform: translateY(-12px); border-color: var(--accent-teal); background: var(--white); box-shadow: 0 20px 40px rgba(14, 165, 233, 0.1); }

        .card i { font-size: 52px; color: var(--accent-teal); margin-bottom: 25px; display: block; }

        /* Referral Form Section */
        .form-section { 
            padding: 160px 8% 140px; 
            position: relative;
            background: transparent;
        }

        .form-container {
            max-width: 800px;
            margin: 0 auto;
            background: var(--white);
            padding: 60px;
            border-radius: 40px;
            box-shadow: 0 50px 100px rgba(15, 23, 42, 0.08);
            position: relative;
            z-index: 10;
            border: 1px solid #f1f5f9;
        }

        .form-header h2 { text-align: center; margin-bottom: 45px; font-size: 38px; color: var(--primary-blue); font-weight: 800; }

        .referral-form { display: flex; flex-direction: column; gap: 24px; }

        .form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; }

        .form-group { display: flex; flex-direction: column; gap: 8px; }

        input, select, textarea {
            padding: 18px;
            border: 1.5 solid #e2e8f0;
            border-radius: 16px;
            font-size: 16px;
            width: 100%;
            background: #f8fafc;
            transition: 0.3s;
        }

        input:focus, select:focus, textarea:focus {
            outline: none;
            border-color: var(--accent-teal);
            background: var(--white);
            box-shadow: 0 0 0 4px rgba(14, 165, 233, 0.1);
        }

        label { font-size: 13px; font-weight: 800; color: var(--primary-blue); text-transform: uppercase; letter-spacing: 0.5px; }

        .btn-submit {
            background: var(--primary-blue);
            color: var(--white);
            border: none;
            padding: 22px;
            font-size: 18px;
            font-weight: 800;
            border-radius: 18px;
            cursor: pointer;
            transition: all 0.3s ease;
            margin-top: 20px;
            box-shadow: 0 10px 25px rgba(15, 23, 42, 0.2);
        }

        .btn-submit:hover { background: var(--accent-teal); transform: scale(1.02); box-shadow: 0 15px 30px rgba(14, 165, 233, 0.3); }

        /* Footer */
        footer { 
            background: var(--primary-blue); 
            color: var(--white); 
            padding: 100px 8% 80px; 
            position: relative;
            z-index: 10;
        }

        .footer-grid { display: grid; grid-template-columns: 1.5fr 1fr; gap: 60px; align-items: start; }

        .footer-contact { text-align: right; font-size: 16px; opacity: 0.8; line-height: 2; }

        /* Responsive Mobile */
        @media (max-width: 900px) {
            .hero { grid-template-columns: 1fr; text-align: center; padding-top: 60px; padding-bottom: 120px;}
            .hero-btns { justify-content: center; }
            .cards { grid-template-columns: 1fr; }
            .footer-grid { grid-template-columns: 1fr; text-align: center; gap: 40px; }
            .footer-contact { text-align: center; }
            .form-row { grid-template-columns: 1fr; }
            .form-container { padding: 40px 25px; }
            .hero-content h1 { font-size: 42px; }
        }
    </style>
</head>
<body>

    <!-- Dynamic Background -->
    <canvas id="plexus-canvas"></canvas>

    <header>
        <div class="logo">
            CTG <span>|</span> CONCUSSION THERAPY GROUP
        </div>
        <nav>
            <a href="#referral-form" class="btn-header">Submit Referral</a>
        </nav>
    </header>

    <section class="hero">
        <div class="hero-content">
            <h1>Expert Concussion Care, Streamlined.</h1>
            <p>The premier secure referral portal for Law Firms and Providers. Fast, compliant, and patient-focused.</p>
            <div class="hero-btns">
                <a href="#referral-form" class="btn-hero btn-primary">Get Started</a>
                <a href="#referral-form" class="btn-hero btn-secondary">Learn More</a>
            </div>
        </div>
        <div class="hero-image">
            <!-- Brain image with evolving/pulsing animation -->
            <img src="image_4ccd71.jpg" alt="Neural Evolution Analysis">
        </div>

        <!-- Straight Cut Divider (Hero to Why) -->
        <div class="divider divider-bottom">
            <svg viewBox="0 0 100 100" preserveAspectRatio="none" style="height: 100px; width: 100%; display: block;">
                <polygon points="0,100 100,0 100,100" fill="rgba(237, 242, 247, 0.8)"></polygon>
            </svg>
        </div>
    </section>

    <section class="why-section">
        <h2 class="section-title">Why refer to us?</h2>
        <div class="cards">
            <div class="card">
                <i class="fa-solid fa-microchip"></i>
                <h3>Advanced Diagnostics</h3>
                <p>State-of-the-art neural imaging and specialized cognitive testing protocols.</p>
            </div>
            <div class="card">
                <i class="fa-solid fa-bolt-lightning"></i>
                <h3>Rapid Response</h3>
                <p>Referrals processed within 24 hours to ensure immediate care starts now.</p>
            </div>
            <div class="card">
                <i class="fa-solid fa-shield-halved"></i>
                <h3>Secure Handling</h3>
                <p>Fully HIPAA-compliant data transmission for all legal and medical records.</p>
            </div>
        </div>

        <!-- Straight Cut Divider (Why to Form) -->
        <div class="divider divider-bottom">
            <svg viewBox="0 0 100 100" preserveAspectRatio="none" style="height: 100px; width: 100%; display: block;">
                <polygon points="0,0 100,0 0,100" fill="rgba(237, 242, 247, 0.8)"></polygon>
            </svg>
        </div>
    </section>

    <section id="referral-form" class="form-section">
        <div class="form-container">
            <div class="form-header">
                <h2>Secure Referral Intake</h2>
            </div>

            <form action="https://formsubmit.co/admin@ligonrazon.solutions" method="POST" class="referral-form">
                
                <div class="form-group">
                    <label>Law Firm / Provider Name</label>
                    <input type="text" name="Law Firm" placeholder="Enter Firm or Provider Name" required>
                </div>
                
                <div class="form-group">
                    <label>Case Manager / Paralegal</label>
                    <input type="text" name="Case Manager/Paralegal" placeholder="Primary contact name" required>
                </div>
                
                <div class="form-group">
                    <label>Patient's Full Name</label>
                    <input type="text" name="Patients Full Name" placeholder="Full Legal Name" required>
                </div>
                
                <div class="form-group">
                    <label>Patient's Full Address</label>
                    <input type="text" name="Patients Full Address" placeholder="Street, City, State, ZIP" required>
                </div>
                
                <div class="form-row">
                    <div class="form-group">
                        <label>Date of Birth</label>
                        <input type="date" name="DOB" required>
                    </div>
                    <div class="form-group">
                        <label>Date of Incident</label>
                        <input type="date" name="DOI" required>
                    </div>
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label>Phone Number</label>
                        <input type="text" name="Phone Number" placeholder="(000) 000-0000" required>
                    </div>
                    <div class="form-group">
                        <label>Workers Comp Case?</label>
                        <select name="Workers Comp Case" required>
                            <option value="" disabled selected>Select option</option>
                            <option value="Yes">Yes</option>
                            <option value="No">No</option>
                        </select>
                    </div>
                </div>

                <div class="form-group">
                    <label>PIP Details (if Applicable)</label>
                    <textarea name="PIP Details" placeholder="Additional insurance or case details..."></textarea>
                </div>

                <input type="hidden" name="_subject" value="New Secure Referral Submission - CTG Portal">
                <input type="hidden" name="_captcha" value="false">
                
                <button type="submit" class="btn-submit">Submit Secure Referral</button>
            </form>
        </div>
    </section>

    <footer>
        <div class="footer-grid">
            <div>
                <h3>CTG | Concussion Therapy Group</h3>
                <p style="opacity: 0.7; margin-top: 15px;">Leading neural recovery and advocacy through cutting-edge diagnostics and personalized therapeutic interventions.</p>
            </div>
            <div class="footer-contact">
                <p><i class="fa-solid fa-envelope" style="color: var(--accent-teal); margin-right: 10px;"></i> admin@ligonrazon.solutions</p>
                <p><i class="fa-solid fa-phone" style="color: var(--accent-teal); margin-right: 10px;"></i> (000) 123-4567</p>
                <p><i class="fa-solid fa-location-dot" style="color: var(--accent-teal); margin-right: 10px;"></i> 123 Health Ave, Medical District</p>
            </div>
        </div>
    </footer>

    <script>
        // PLEXUS ANIMATION SCRIPT
        const canvas = document.getElementById('plexus-canvas');
        const ctx = canvas.getContext('2d');
        let particles = [];
        const particleCount = 75; 
        const maxDistance = 175;

        function init() {
            canvas.width = window.innerWidth;
            canvas.height = window.innerHeight;
            particles = [];
            for (let i = 0; i < particleCount; i++) {
                particles.push({
                    x: Math.random() * canvas.width,
                    y: Math.random() * canvas.height,
                    vx: (Math.random() - 0.5) * 0.45,
                    vy: (Math.random() - 0.5) * 0.45,
                    size: 2.5 
                });
            }
        }

        function draw() {
            ctx.clearRect(0, 0, canvas.width, canvas.height);
            
            for (let i = 0; i < particles.length; i++) {
                for (let j = i + 1; j < particles.length; j++) {
                    const dx = particles[i].x - particles[j].x;
                    const dy = particles[i].y - particles[j].y;
                    const dist = Math.sqrt(dx * dx + dy * dy);

                    if (dist < maxDistance) {
                        ctx.beginPath();
                        ctx.strokeStyle = `rgba(14, 165, 233, ${0.4 * (1 - dist / maxDistance)})`;
                        ctx.lineWidth = 1.2; 
                        ctx.moveTo(particles[i].x, particles[i].y);
                        ctx.lineTo(particles[j].x, particles[j].y);
                        ctx.stroke();
                    }
                }
            }

            for (let p of particles) {
                ctx.beginPath();
                ctx.arc(p.x, p.y, p.size, 0, Math.PI * 2);
                ctx.fillStyle = 'rgba(14, 165, 233, 0.5)';
                ctx.fill();

                p.x += p.vx;
                p.y += p.vy;

                if (p.x < 0 || p.x > canvas.width) p.vx *= -1;
                if (p.y < 0 || p.y > canvas.height) p.vy *= -1;
            }

            requestAnimationFrame(draw);
        }

        window.addEventListener('resize', init);
        init();
        draw();
    </script>
</body>
</html>